#ifndef GLOBALS_H
#define GLOBALS_H

enum STATE{LOADING, MENU, ABOUT, GAME, CLOSE};

const int ScreenHeight = 600;
const int ScreenWidth = 800;

static int TICS_PER_SEC = 50;
static int FRAMES_PER_SEC = 30;

#endif // GLOBALS_H
